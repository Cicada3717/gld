[build]
builder = "nixpacks"

[deploy]
startCommand = "python replay.py && python zone_paper_trader.py --ticker GC=F --capital 10000 & streamlit run dashboard.py --server.port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
