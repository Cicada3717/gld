[build]
builder = "nixpacks"

[deploy]
startCommand = "python lfv_paper_trader.py --capital 10000 & streamlit run lfv_dashboard.py --server.port $PORT --server.address 0.0.0.0"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
