[build]
builder = "nixpacks"

[deploy]
startCommand = "python paper_trader.py --capital 500 & python zone_paper_trader.py --capital 500 & streamlit run dashboard.py --server.port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
